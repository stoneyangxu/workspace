import { MyDropdownButtonDirective } from './my-dropdown-button.directive';

describe('MyDropdownButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDropdownButtonDirective();
    expect(directive).toBeTruthy();
  });
});
