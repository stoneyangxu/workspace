import { DtPlaceholderDirective } from './dt-placeholder.directive';

describe('DtPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new DtPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
